# Sample: anonymous-42-minimal

**Category:** Minimal Draft (SFW)
**tip_personality:** `default`
**persona_id:** `ern:1.0:01HXY00000000000000000000M`

## Notes

- This is the **smallest valid Draft** that satisfies all v1.0 schema constraints.
- `name`, `tagline`, and `greeting` use the single character `?`; `description` is one short sentence.
- `definition_dialogues` is an empty array `[]` — the minimum allowed count is 0.
- `affinity_priors` is omitted entirely (it is optional).
- `system_prompt` is a single short sentence.
- Useful as a baseline for schema compliance testing and for creators who want a blank-slate persona.
